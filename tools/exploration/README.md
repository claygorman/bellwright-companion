# Historical reverse-engineering scripts

Python scripts used while cracking the save format (protobuf walking, name-table
digging, first NPC extraction). Superseded by the TypeScript parser in
`parser/` — kept for archaeology and as worked examples for extending
`docs/save-format.md`. Not maintained.
